{
    "name": "emberapi-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "🔥 EmberAPI - A blazing fast Node.js framework with fire-themed APIs",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "test": "turbo run test",
        "lint": "turbo run lint",
        "clean": "turbo run clean && rm -rf node_modules"
    },
    "devDependencies": {
        "turbo": "^1.11.2",
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}